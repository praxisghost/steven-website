# Migration Progress — stevenlegg.xyz

Canonical state / changelog. Single source of truth. Read this first every run.

## Current phase
**Phase 4 COMPLETE & signed off (run 36)** — design system LOCKED; WCAG-AA verified (axe 0 violations, light/dark × mobile/desktop) + Lighthouse mobile a11y 100 / SEO 100 / best-practices 96 / perf 98. **Phase 5 (Railway deploy) — artifacts PREPARED (run 36), execution HIGH-risk / user-gated.** Only remaining gate before launch: human-run deploy + Lighthouse-perf on the live URL. (Phases 1–3 complete; Phase 2 backend deploy-ready.)

## Status
- **Completed:** Planning files (run 1). Scaffold (run 2). Deps + typecheck (run 3). Content map (run 4). **Home + all 12 primary nav pages (run 5):** built `frontend/app/{about,career,contact,writing,art,book-reviews,language-learning,media,projects,resources,sports,misc}/page.tsx` + home, with shared `SiteHeader`/`SiteFooter`/`PageHeader`/`HubGrid` + `lib/nav.ts`. Source copy preserved (about & sports full prose; hubs as card grids). `tsc --noEmit` EXIT 0.
- **Current:** ~668 LOC across 13 routes + 4 components. LOC cap intentionally exceeded this run per explicit user instruction to disregard the per-run limit. `next build`/`next lint` still BLOCKED in sandbox (SWC bus-error, exit 135) — tsc is the available verification.
- **Done (run 6):** Phase 3 step 2 — migrated flagship articles. 6 blog posts + 5 project pages parsed from source HTML into faithful JSON (`frontend/content/{blog,projects}/*.json`), rendered via `/blog`, `/blog/[slug]`, `/projects/[slug]` + reworked `/projects` index (now links real pages). Added `lib/content.ts` (fs loader, blog sorted newest-first) + `ArticleBody` renderer. Writing→Blog card now links. `tsc --noEmit` EXIT 0.
- **Done (run 7):** Phase 3 step 3 — asset migration. Copied `/website/public/img/*` (17 folders, ~25MB), 2 audio mp3s, and 2 xlsx templates into `frontend/public/{img,audio,downloads}` (xlsx renamed to match existing Misc links). Added `lib/gallery.ts` (fs image lister, filters README/non-images, prettifies alt text) + `components/Gallery.tsx` (responsive 4:3 lazy-loaded grid). Wired real content: Media (dogs photos + 2 audio players + playlists grid + Letterboxd link), Misc (frogs gallery), About (salamanders gallery). Added root `.gitignore`. `tsc --noEmit` EXIT 0.
- **Done (run 8):** Phase 3 self-repair — **restored the missing `frontend/content/{blog,projects}/*.json`** (6 blog + 5 project files). These back `/blog`, `/blog/[slug]`, `/projects`, `/projects/[slug]` via `lib/content.ts` but were ABSENT on disk (logged created run 6, never persisted) — routes would have failed at build. Re-parsed faithfully from `/website/public/{blog,projects}/*.html` with a bs4 script (title, date, h2/h3/p/li/quote + figcaptions→italic; footnote `<sup>` refs stripped, `↩` removed). Validated all 11 JSON load + shape via node; `tsc --noEmit` EXIT 0. Note: hero/figure images in posts are not captured as blocks (ArticleBody is text-only) — consistent with prior approach; images live in galleries.
- **Done (run 9):** Phase 3 step 4a — migrated the **9 top-level Technology pages** (ai-prompts, artificial-intelligence, foldable-phones, linux, macos, return-to-linux, smartphone-design, software-tutorials, windows) to `frontend/content/technology/*.json` (bs4 parse from `/website/public/technology/*.html`; h2/h3/p/li/quote, footnote sup stripped). Generalized `lib/content.ts` `Collection` type to include `technology`; added `/technology` index + `/technology/[slug]` (reuse `ArticleBody`). Wired Writing→Technology card to `/technology`. 9 JSON shape-checked + `tsc --noEmit` EXIT 0. **Split:** the `technology/linux/` & `technology/software-tutorials/` subdir pages were NOT migrated this run (those top-level pages are hub/index stubs — 1–7 blocks — pointing to the subdirs); queued as 4b.
- **Done (run 10):** Phase 3 step 4b — migrated the **12 Technology subdir pages** (linux/{arch,debian,fedora-kde,fedora-kde-guide,fedora-kde-experience,mint}; software-tutorials/{audacity,darktable,gimp,kdenlive,notion,obsidian}) to `frontend/content/technology/{linux,software-tutorials}/*.json` (bs4 parse from source, h2/h3/p/li/quote, footnote sup stripped, figcaption→"— " note). Added `getSubCollection`/`getSubArticle` + `SubParent` to `lib/content.ts`; new `SubHub`/`SubArticle` components; nested routes `/technology/linux`, `/technology/linux/[slug]`, `/technology/software-tutorials`, `/technology/software-tutorials/[slug]`. The two hubs now have dedicated static pages linking each child, and are excluded from `technology/[slug]` `generateStaticParams` to avoid path conflicts. 12 JSON shape-checked + `tsc --noEmit` EXIT 0.
- **Done (run 11):** Phase 3 step 4c — migrated **Book Reviews + Self Improvement + Retro Gaming** (5 articles: midnight-library, atomic-habits, gameboy-advance-sp, nintendo-gamecube, playstation-2) to `frontend/content/{book-reviews,self-improvement,retro-gaming}/*.json` (bs4 parse from source; h2/h3/p/li/quote, footnote sup stripped, figcaption→"— "). Extended `Collection` type with the 3 names. Added reusable `CollectionIndex` + `CollectionArticle` components; rebuilt `/book-reviews` (hub now links real pages) and added `/book-reviews/[slug]`, plus new `/self-improvement` & `/retro-gaming` hubs + `[slug]` routes. Wired the Writing→Retro Gaming & Self Improvement cards (were dead). 5 JSON shape-checked + `tsc --noEmit` EXIT 0.
- **Done (run 12):** Phase 3 step 4d — migrated the **-isms set (306 pages**, not 104 as the map estimated) from `/website/public/isms/*.html`. Source pages are ALL placeholders ("content coming soon"; the lone outlier `superorganism.html` is a JS-graph stub, also no prose) — so rather than 306 near-empty JSON files, stored one `frontend/content/isms.json` manifest (slug/title/status ×306). Added `lib/isms.ts` loader, client `IsmsIndex` (search filter + alphabetical A–Z grouping), `/isms` index + `/isms/[slug]` (renders preserved placeholder, `generateStaticParams` ×306). Wired Writing→-isms card (was dead). Manifest shape-checked (306) + `tsc --noEmit` EXIT 0.
- **Done (run 13):** Phase 3 (Language Learning) — **migrated the 57 pronunciation guides** from `/website/content/pronunciation-guides/generated/*.md` (incl. ```` ```md ````-fence-wrapped variants) into `frontend/content/pronunciation/{<slug>.json + index.json}`. python-markdown (tables + sane_lists) → HTML, fences stripped, titles/l1/l2 from `metadata/index.json`. Added `lib/pronunciation.ts`, client `PronIndex` (searchable, A–Z grouped — Hick's Law), routes `/language-learning/pronunciation` + `/[slug]` (renders trusted first-party HTML via scoped `.guide` prose CSS incl. IPA tables), and wired the language-learning stub's "Pronunciation Guides" card (was dead) to the real index. Also fixed `isms/[slug]` to async `params` (Next 15 build-breaker plain tsc misses). 57 JSON + index shape-checked; `tsc --noEmit` EXIT 0. **Note:** `content/vocabulary/generated/` is EMPTY (0 md) — nothing to migrate there yet; re-check next run.
- **Done (run 15):** Phase 3 step 4e — migrated the **7 Writing sub-sections** (animals, criticisms, politics, questions, rhythm-and-meter, shavian-english, tributes) to `frontend/content/writing/*.json`. Source mix: 5 are real prose (animals, questions, rhythm-and-meter, tributes, shavian-english) and 2 are curated topical hubs into already-migrated Blog posts (criticisms→american-education-what-we-owe; politics→trump-epstein-photos + divide-and-the-road-ahead). Bilingual **shavian-english.html** handled specially: extracted clean English from `.gloss-line .en` spans (parallel `.sh` Shavian retained in a `shavian` JSON field, plus a leading note), so no content lost. Extended `Article` with optional `links: CuratedLink[]` and added `writing` to the `Collection` union in `lib/content.ts`; added one route `app/writing/[slug]/page.tsx` (async params, renders `ArticleBody` + optional curated-link cards) reusing existing components. Wired **all 7 previously-dead Writing hub cards** with hrefs + notes. 7 JSON shape-checked + `tsc --noEmit` EXIT 0.
- **Done (run 17):** Phase 2 batch 3 — **write API endpoints** mirroring website3.0 (legacy server.ts parity), MINUS Supabase. Added `api/views.py` (views/contact/newsletter + `api_not_found` JSON-404 catch-all + `_clean` sanitize + email validation), `api/throttling.py` (per-IP `Views/Contact/Newsletter` throttles = 60/min, 10/hr, 10/hr), wired `api/urls.py` (catch-all last), added `REST_FRAMEWORK["DEFAULT_THROTTLE_RATES"]` + `NUM_PROXIES` to `config/settings.py`, and 7 endpoint tests in `api/tests.py`. **Divergence from literal "DRF serializers" plan wording:** the reference uses function-based hand-rolled validation (matching the old `sanitize()`), so kept that for parity rather than introducing serializers for tiny payloads — noted as a deliberate choice. ~145 code LOC. Verified on a local-disk copy: `manage.py check` 0 issues; **`manage.py test` 11/11 PASS**. (Dropped the Supabase `me` endpoint + JWT auth entirely.)
- **Done (run 18):** Phase 3 step 4f-i — wired the **Language-Learning "Methods & Tools"** stub: migrated 9 method guides (anki-cloze-cards, anki-guide, comprehensible-input, extensive-reading, gold-list, hypertts, shadowing, task-based-output, tprs) from `/website/public/*.html` → `frontend/content/language-learning/methods/*.json` (bs4: article body, strip `ol.lang-toc`/nav, sup footnotes). Added generic `getDirCollection`/`getDirArticle` to `lib/content.ts`, `/language-learning/methods` hub (reuses `SubHub`) + `[slug]` route, and pointed the dead card at it. `tsc --noEmit` EXIT 0. Note: `content/vocabulary/generated/` still EMPTY (0 md) — nothing to migrate.
- **Done (run 19):** Phase 3 step 4f-ii — migrated the **6 Con-Lang guides** (esperanto/novial/ido/interslavic/klingon/interlingua) from the 6 individual source pages (`con-lang.html` is just a hub stub; real prose lives in `/website/public/{esperanto,novial,ido,interslavic,klingon,interlingua}.html`). Parsed via bs4 (strip `ol.lang-toc`, footnote `<sup>` refs) into `frontend/content/language-learning/con-lang/*.json` using the generic `getDir*` loader. These pages are **table-heavy** (alphabet/IPA + grammar tables — 101 tables total across the 6), so to preserve all content I added an **additive `table` block type** to `Block` (`{type:"table"; headers; rows}`) in `lib/content.ts` and render it in `ArticleBody` (responsive `overflow-x-auto` table). Added a `leadText(blocks)` helper (skips non-text blocks) + updated the 5 card-preview call sites (SubHub, CollectionIndex, projects, technology) that previously did `blocks.find(b=>b.type==="p").text` — the union now requires narrowing. Added `/language-learning/con-lang` hub (reuses `SubHub`) + `[slug]` route (async params) and wired the dead "Con-Lang" card on the language-learning hub. 6 JSON + 101 tables shape-checked; `tsc --noEmit` EXIT 0. **Non-Latin scripts preserved** (Klingon `tlhIngan Hol`, IPA, Esperanto diacritics) — verified.
- **Done (run 20):** Phase 3 dead-card sweep — **wired all 16 previously-dead `/resources` cards** (every card had NO `href`). Software Guides → `/technology/software-tutorials/{audacity,darktable,gimp,kdenlive,notion,obsidian}`; Methods & Tools → `/language-learning/methods/{anki-cloze-cards,anki-guide,comprehensible-input,extensive-reading,gold-list,hypertts,shadowing,task-based-output,tprs}`; Language Guides → `/language-learning`. All 16 targets confirmed on disk (`[slug]` routes + JSON exist). Refactored `Group` to take `HubItem[]` (hrefs) instead of `string[]`. Audit of the other three hubs: `/writing` already fully wired (12/12, runs 11/12/15); `/media` content-wired (galleries+audio) — only soft item is a generic `https://letterboxd.com/` URL (no real profile handle available; left as-is); `/career` source lists were JS-rendered (not static) → still placeholder, queued. **Guitar Learning Guide** card left as a noted non-link (source `/website/public/guitar.html` not yet migrated) to avoid a 404. ~55 LOC, 1 file. `tsc --noEmit` EXIT 0.
- **Done (run 21):** Phase 3 — migrated the last dead card, **`guitar.html`** (+ its 731-line `guitar-scale.js`). New `app/resources/guitar/page.tsx` route; `components/GuitarGuide.tsx` (client) renders the full scale-by-key roadmap (9 keys × 4 scales, "Current" pill on E Major Pentatonic), the YouTube reference embed, the **interactive E Major Pentatonic scale player** (Play Scale / Play Melody w/ 3 variations + count-in / Stop) with the synchronized SVG treble-staff notation, and the Study Philosophy section. Ported the Web-Audio engine faithfully to typed `lib/guitarScale.ts` (`setupGuitarScale()` → teardown; same scheduler/lookahead, plucked-string envelope, melody mode). **Visual divergence:** restyled the source's dark theme to the light "Editorial Minimal" tokens (active/playing state = accent `#0F766E`, not the original green). Wired the `/resources` Guitar card (was a noted non-link) → `/resources/guitar`. `tsc --noEmit` EXIT 0. ~730 new LOC (indivisible interactive widget; cap waived per prior user instruction).
- **Done (run 23):** Phase 4 polish #1 — **global header accessibility + wayfinding.** Added (a) a skip-to-content link in `app/layout.tsx` (WCAG 2.4.1 Bypass Blocks; `sr-only`→`focus:not-sr-only`, jumps past the 12-item nav) targeting a focusable `<div id="main" tabIndex={-1}>` wrapper — deliberately a div, NOT `<main>`, because every page already renders its own `<main>` landmark (a second would nest/duplicate it & fail single-main); (b) `aria-current="page"` + active style (`font-medium text-accent`) on the current nav item via a new `components/NavLinks.tsx` client component (`usePathname`; `startsWith` so `/writing/x` lights "Writing") — isolates the client boundary so `SiteHeader` stays a server component; (c) a real **visible focus ring** (`focus-visible:ring-2 ring-accent ring-offset-2`) on every nav link + the brand link, replacing the prior color-only `focus-visible:text-accent` (insufficient for WCAG 2.4.7). ~55 LOC across 3 files (1 new). `tsc --noEmit` EXIT 0; no internal raw-anchor build-breakers (verified). **COMMIT BLOCKED** — stale `.git/index.lock` AND `.git/HEAD.lock` pre-existed this run (mount denies unlink: `rm` → "Operation not permitted"), so even `git add` failed. Worked around the index via `GIT_INDEX_FILE=/tmp` + `commit-tree` → commit object `995c1f9` was written to the object store, but `update-ref` couldn't advance the branch (HEAD.lock). Files SAVED to disk, UNCOMMITTED (see Needs Human Approval).
- **Done (run 24):** Phase 4 polish #2 — **responsive disclosure nav.** Rewrote `frontend/components/NavLinks.tsx` (single file, ~80 LOC): below `lg`, the flat 12-item set (which wrapped 3–4 rows deep on ≤360px / ~280px Fold-cover screens) collapses behind one hamburger `<button>` (`☰`/`✕`) revealing a stacked, full-width-row list; at `lg+` the nav is unchanged (inline, right-aligned via `lg:ml-auto`, wrapping as before). Accessible: `aria-expanded`, `aria-controls="primary-nav"`, dynamic `aria-label`, Escape-to-close, auto-close on route change (`usePathname` effect); existing focus ring + `aria-current` retained on every target. No `SiteHeader` change needed (already renders `<NavLinks/>` in its `flex-wrap` row; button `ml-auto` + nav `basis-full` handle wrapping). `tsc --noEmit` EXIT 0. Commit BLOCKED (same pre-existing `.git/index.lock`+`HEAD.lock`; mount denies unlink). File SAVED to disk, UNCOMMITTED.
- **Done (run 25):** Phase 4 polish #3 (verification half) — **static build-breaker gate + a11y/responsive audit of the run-23/24 nav delta** (verification only; no source changed). Confirmed the three breakers `tsc` misses are absent: no raw internal page `<a href="/…">` (the two `app/misc` hits are `/downloads/*.xlsx` static-file downloads — correct as plain anchors, files on disk; untouched this run), single-`<main>` invariant holds (`app/layout.tsx` renders `<div id="main">`, **zero** `<main>` elements — the 2 grep hits are comment text), and the changed files use no dynamic `params`. `tsc --noEmit` EXIT 0. A11y audit of `NavLinks.tsx`/`SiteHeader.tsx`/`layout.tsx` PASS: skip-link→focusable `#main`, dynamic `aria-label`+`aria-expanded`+`aria-controls="primary-nav"` (id matches `<nav>`), Escape + route-change auto-close, focus ring on every Link/button/brand, `lg:` flips hamburger↔inline, full-width `py-2` rows (Fitts); no vestibular motion (color-only transitions). **Real `next build` NOT re-run** — sandbox triple-blocked (local `/` only 438M free; Google Fonts unreachable for `next/font/google`; `node_modules` swc = darwin = wrong platform); last real build GREEN at run 14 (422/422 pages) and this delta is `tsc`-clean + breaker-free. Lighthouse/Playwright deferred (no headless Chrome). **COMMIT: §6 resilience — built a fresh commit object** including all stranded run-23/24 files + this entry (locks still block `update-ref`); SHA + one-line fix in Needs Human Approval and in `PENDING_REF_RECOVERY.txt`.
- **Done (run 26):** Ref advanced by user (runs 23–25 landed at HEAD `9537a66`). **(a) Integrity self-repair:** the recovery command's trailing `git checkout .` had restored `app/layout.tsx` + `components/SiteHeader.tsx` from the **stale real index** (runs 23–25 were committed via a temp `/tmp/idx`, so `.git/index` never held them) — silently reverting both to their pre-a11y content on disk while HEAD was correct. Restored both from HEAD via in-place redirect (`git show HEAD:path > path`; the mount denies `unlink` even on working-tree files, so `git checkout`/`reset --hard` can't run in-sandbox). Skip-link + `div#main` + NavLinks delegation confirmed back. **(b) Phase 4 polish #4 — `Gallery.tsx` `<img>` → `next/image`** (fill + per-`cols` `sizes` + `quality=85`, wrapped in a `relative aspect-[4/3]` box to reserve space → no CLS); resolves the lone `no-img-element` lint. Added `output: "standalone"` (§4 Railway requirement, previously missing) + `images.formats: [avif, webp]` to `next.config.ts`. `tsc --noEmit` EXIT 0; no raw `<img>` remain; Gallery consumers (about/media/misc) are server components (next/image OK). Real `next build` still env-blocked (darwin swc + fonts + 438M disk); last GREEN run 14. ~40 LOC, 2 files (+ progress).
- **Done (run 27):** Phase 4 polish #5 — **branded error / empty-state system (§8) + health contract (§16/§17).** Run 26 confirmed landed (user advanced ref to `4e9f611`; `PENDING_REF_RECOVERY.txt` deleted; working tree clean). New reusable client `components/ErrorState.tsx` (human-readable message, Request ID surfaced from `X-Request-ID`/Next digest, **"Copy error details"** → URL+timestamp+requestId+userAgent to clipboard with `aria-live` "Copied", optional retry + always a "Go to homepage" link, optional DNS/SSL self-diagnostic hint pointing to **Railway → Settings → Domains + `_railway-verify` DNS TXT** — NOT a `/.well-known` path). Wired via `app/error.tsx` (client segment boundary, code 500, `reset`→retry, logs `requestId`/`digest`, `showInfraHint`) and `app/not-found.tsx` (branded 404, no retry). Added same-origin `app/healthz/route.ts` (server-side fetch of Django `/api/health/` via **server-only `API_URL`** → 200 only when reachable, 503 otherwise, forwards `X-Request-ID`, 3s abort timeout, `force-dynamic`) and client `components/HealthBanner.tsx` (calls `/healthz` on mount; dismissible `role="status"` degraded banner; renders null when healthy; mounted in `layout.tsx` after the skip-link, before header). Each error page renders exactly one `<main>` (layout wrapper stays `div#main`). `tsc --noEmit` EXIT 0; build-breaker scan clean (no raw internal `<a>`, single-`<main>` invariant holds, no async-`params`). ~5 files new + layout edit, ~190 LOC (indivisible error+health unit; cap waived per standing user instruction).
- **Done (run 28):** Phase 4 polish #6 (§8) — **light/dark theme system.** Adopted + verified the stranded run-28 working-tree delta (a prior interrupted attempt had left it on disk, unverified/unlogged/uncommitted) rather than reverting it (§9 reverts only on *failure* — this was sound). Converted the 6 semantic color tokens in `tailwind.config.ts` from literals to `rgb(var(--color-*) / <alpha-value>)` (keeps Tailwind opacity modifiers working) and defined light + **AA-lifted dark** values (accent `#0F766E`→`#2DD4BF` on dark bg) as channel-triplet CSS vars in `app/globals.css` (`:root` + `.dark`). Added accessible `components/ThemeToggle.tsx` (real `<button>` + `aria-pressed` + dynamic `aria-label`; emoji `aria-hidden`; render-null-until-mounted to avoid hydration mismatch) wired into `SiteHeader`, plus a **no-FOUC inline `<head>` init script** in `layout.tsx` (reads `localStorage.theme`, falls back to `prefers-color-scheme`, sets `.dark` pre-paint) with `suppressHydrationWarning`. Whole site flips via the `.dark` class — zero per-component `dark:` churn. `tsc --noEmit` EXIT 0; build-breaker scan clean (no raw internal `<a>`, single-`<main>` invariant holds — layout wrapper is `div#main`, the 2 grep hits are comments, no async-`params`). Real `next build` still env-blocked (darwin swc + fonts + disk), last GREEN run 14. ~65 LOC, 4 files changed + 1 new (+ progress).
- **Done (run 29):** Integrity + verification pass (no source changed; build/Lighthouse env-blocked + locks block commit, so chose the changelog-requested re-verify). **Integrity GREEN:** all **342** HEAD-tracked files present on disk, **0 missing** — no silent drop like run 6's; the only working-tree delta vs HEAD is the expected uncommitted run-28 doc note + stale-index noise (`ThemeToggle.tsx` shows `??`/`D` but is **byte-identical to HEAD** — verified via `diff` against `HEAD:` blob). `tsc --noEmit` **EXIT 0**. **Build-breaker scan clean:** no raw internal `<a href="/…">` (only `/downloads/*.xlsx` static anchors), single-`<main>` invariant holds (`layout.tsx` = `<div id="main">`; its 2 grep hits are the skip-link `href="#main"` + a comment), and **all 14** dynamic `[slug]` routes type `params: Promise<…>` (async-OK). Locks still present (3× 0-byte, unlink denied) → commit deferred per the standing approval item; this run is correctly no-commit (§6.6).
- **Done (run 30):** Continuity self-repair — **landed the stranded run-29 log + cleared the lock alarm.** Re-assessed the "3 stale locks": only **`.git/index.lock`** (0-byte) still exists — `.git/HEAD.lock` and `.git/refs/heads/website-xyz.lock` are **already gone**. Confirmed the fuse mount denies `unlink` (so `rm` fails) but allows file *writes/renames*, and **proved commits still land** via the temp-index route (`GIT_INDEX_FILE=/tmp/idx git read-tree HEAD` → overlay only `MIGRATION_PROGRESS.md` → `commit-tree` → `update-ref`). Clean-tree check: temp tree vs HEAD differs **only** in `MIGRATION_PROGRESS.md` (342/342 files both sides; `ThemeToggle.tsx` present & byte-identical — the `??`/`D` was pure stale-`.git/index` noise). No source changed; the lingering `index.lock` is **not** a hard commit blocker. ~0 code LOC (doc/QA only).
- **Done (run 31):** Phase 4 polish #7 (§6 a11y/§5) — **programmatic WCAG-AA contrast audit of both theme palettes + fix.** Computed WCAG 2.1 relative-luminance contrast ratios for every meaningful token pairing in light AND dark (from `globals.css`). Light & dark body/secondary/muted/accent text all PASS ≥4.5:1; accent-as-UI PASS ≥3:1. **Caught one real 1.4.3 failure:** `text-white` on `bg-accent` = only **1.86:1 in dark** (accent lifts to bright teal `#2DD4BF` there) — used on both form submit buttons (`ContactForm`/`NewsletterForm`) + the GuitarGuide "Current" pill. Fixed all three `text-white`→`text-bg` (matching the already-correct `ErrorState` pattern): now **5.24:1 light / 10.36:1 dark** — AA for normal text in both. `accent-soft` light-mode 2.38:1 is moot (token defined but never used as a class). `tsc --noEmit` EXIT 0; no `text-white` remain. ~3 one-token edits, 3 files. **Logged (not fixed):** input-field hairline borders are ~1.2–1.35:1 vs bg (1.4.11 UI-boundary gap) — design-token decision deferred to its own run w/ sketched options (see Needs Human Approval).
- **Done (run 32):** Phase 4 polish #8 (§5/§6) — **fixed the run-31 input-border WCAG 1.4.11 gap.** Two options sketched: **(A, chosen)** a dedicated `--color-field` token applied ONLY to interactive form controls, vs **(B, rejected)** bumping the global `--color-hairline` token — B would heavy every decorative card/divider/footer rule sitewide and break the "Editorial Minimal" aesthetic, and 1.4.11 exempts purely decorative borders, so B over-applies. Added `--color-field` (light `#86868C`, dark `#6E6E74` — both **AA-tuned to just clear 3:1**, not darker, to stay minimal) in `globals.css` (`:root`+`.dark`) + `field` color in `tailwind.config.ts`, then swapped `border-hairline`→`border-field` on exactly the **6 form controls / 4 files** (`ContactForm` shared INPUT [2 inputs+textarea], `NewsletterForm`, `IsmsIndex` search, `PronIndex` search); `focus:border-accent` retained. Decorative hairlines (cards, dividers, header/footer, the ContactForm success box, PronIndex result cards) **untouched**. Programmatic WCAG check: field border now **3.46:1 light / 3.81:1 dark** (was 1.20 / 1.35); accent focus 5.24 / 10.36 still pass. `tsc --noEmit` EXIT 0; build-breaker scan clean (no raw internal `<a>`; only the 6 control sites use `border-field`). ~9 LOC, 6 files. Real `next build`/Lighthouse still env-blocked (sandbox darwin swc / no headless Chrome), last GREEN run 14.
- **Done (run 33):** Phase 4 — **re-ran a REAL `next build` (not just tsc) and confirmed runs 26–28 are GREEN.** The build was the long-standing "user-machine-only" blocker because the sandbox `node_modules/@next/swc-darwin-arm64` is the wrong-platform binary (sandbox is linux-aarch64); replicated run-14's fix in a throwaway local-disk copy: hard-fetched `@next/swc-linux-arm64-gnu@15.5.19` (complete 135MB ELF aarch64 `.node`), swapped it in, and stubbed `next/font/google` (Inter/Newsreader → `.variable` stubs — fonts.googleapis.com still returns 403 through the proxy; **real source untouched**, fonts fetch fine on user machine/Railway). Result: **✓ Compiled successfully in ~2.2s; ✓ 449/449 static pages generated; exit 0** (up from run-14's 422 — the extra 27 are runs 15–32 content: writing sub-sections, methods/con-lang guides, etc.). Verified the run-26/27/28 surfaces specifically: `output:standalone` → `.next/standalone` dir PRESENT (run 26); `next/image` Gallery compiled (no `no-img-element` error); `/healthz` dynamic route + `_not-found`/error boundary compiled (run 27); dark-mode toggle compiled clean (run 28). 447 prerendered `.html` on disk. No source changed (verification only); throwaway copy + tarball cleaned up after. **Lighthouse + in-situ WCAG-AA still deferred** — needs headless Chrome (absent in sandbox); contrast already proven programmatically runs 31/32.
- **Done (run 34):** Phase 3 gap-closure (user: "migrate as much as possible, ignore all limits") — a fresh source audit caught **two genuinely-unmigrated content sets** beyond the changelog. **(a) Famous Linguists** (5 SLA-theorist profiles — Krashen, VanPatten, Swain, Long, Ellis — real prose, never migrated): parsed `/website/public/famous-linguists/*.html` (bs4: header h1/subtitle + article h2/p, hero placeholder skipped) → `content/language-learning/famous-linguists/*.json`, added a hub (`/language-learning/famous-linguists`, reuses `SubHub`+`getDirCollection`) + `[slug]` route, and wired a new "Famous Linguists" card on the language-learning hub (was absent). **(b) i18n mirror sites** (the run-4 "deferred, not lost" item): the 4 constructed-language full-site translations — **Ido/io, Interlingua/ia, Interslavic/isv, Novial/nov**, 12 pages each = 48 — parsed `/website/public/{io,ia,isv,nov}/*.html` into `content/i18n/<locale>/{_meta.json + 11 page JSON}` (index→locale meta: tagline, localized nav labels, back-link; content pages→title/sub/blocks). New `lib/i18n.ts` loader + a **locale-prefixed route system**: `app/[locale]/page.tsx` (localized landing: tagline, localized nav grid, an EN "this is the <Name> version → View in English" banner, cross-locale switcher) and `app/[locale]/[page]/page.tsx` (reuses `ArticleBody`), **both `dynamicParams=false`** so only the 4 locales prerender and the dynamic root segment never shadows the real static routes (about/art/blog/…). Added a "Languages: Ido · Interlingua · Interslavic · Novial" switcher to `SiteFooter` for discoverability. Each page carries proper `lang={htmlLang}` (a11y/SEO). **Build: REAL `next build` GREEN (linux-swc workaround) — ✓ compiled, exit 0, 501/501 static pages (was 447 at run 33; +54 = famous-linguists 6 + i18n 48).** `tsc --noEmit` EXIT 0; only `[locale]` is dynamic at root (no segment-name conflict). ~5 new routes/lib + 2 edits; content JSON ×53 (5 famous-linguists + 48 i18n). Source untouched; throwaway build copy cleaned.
- **Done (run 35):** Phase 4 sign-off (a11y half) — **ran a REAL in-situ WCAG-AA audit with headless Chromium** (the long-blocked gate). This sandbox is linux-aarch64 with disk + a reachable npm CDN, so — unlike prior sandboxes — I installed it: `@axe-core/playwright` + Chromium 148 (`curl -C -` resume of the 185 MiB build across timeout windows, since playwright's own downloader gets SIGKILLed at the call boundary), built the throwaway prod copy (linux-swc + font stub, run-33 recipe; **503/503 pages GREEN**), served `.next/standalone`, and ran axe (`wcag2a/2aa/21aa`) over 6 representative pages × **light+dark × mobile(390)+desktop(1366)**. **Caught 2 real violation classes hand-computation missed:** (1) `link-in-text-block` **1.4.1** sitewide — `SiteFooter`/`/contact` `linktr.ee` links were colour-only (accent vs muted **1.03:1**); (2) `color-contrast` **1.4.3** on `/resources/guitar` — `.note-interval`/`.notation-label` `#A8A8A2` **2.28:1**, and in **dark** the light-card widget fell to **1.7–3.64:1** (transparent containers over the dark page). **Fixed all (3 files):** inline links `hover:underline`→`underline underline-offset-2`; widget greys `#A8A8A2`→`#6B6B72`; pinned `.scale-player`/`.notation-wrap` `background:#FAFAF8` (self-contained light card, both themes). **Re-audit: 0 violations across all 4 combos** (raw JSON + `AUDIT_WCAG_AA.md` in `/audit`). Rebuild **503/503 GREEN**. Chromium/throwaway copy are sandbox-only; real source untouched except the 3 fixes. Principle: WCAG 1.4.1/1.4.3 (non-colour cues, visual hierarchy). ~7 LOC, 3 files + audit artifacts.
- `[2026-06-11 18:20] Task: Phase-4 a11y sign-off (axe/Chromium, light+dark×mobile+desktop) | Changed: 3 src ~7 LOC + /audit (5 files) | Principle: WCAG 1.4.1+1.4.3 | Build: pass 503/503 | Status: on track | Confidence: High | Next: Lighthouse perf≥90 on live Railway`
- **Done (run 36):** (user: "no page/token/time/run limits") **two big units in one session.** **(A) SEO foundation** — the site had only bare `title`s, zero descriptions, no OG, no sitemap/robots/manifest, no favicon. Added `lib/seo.ts` (SITE constants + `articleMeta`/`pageMeta` helpers, lead-paragraph excerpt → meta description); enriched root `layout.tsx` (metadataBase, `%s · Steven Legg` title template, default OG/Twitter, robots, `viewport.themeColor`); normalized all 24 hub titles + wired each to `pageMeta` (unique tailored description + **self-canonical** + OG); wired all 18 `[slug]` routes to `articleMeta` (per-article description from content, OG type=article); `-isms` placeholders set `robots:noindex` (thin content). Added `app/sitemap.ts` (196 URLs from the same fs loaders; isms excluded), `app/robots.ts`, `app/manifest.ts`, `app/icon.svg` favicon + generated `public/{icon-192,icon-512,apple-icon}.png` + 1200×630 `public/og-default.png` (PIL, on-brand teal/cream). **Caught + fixed a real bug:** layout-level `canonical:"/"` was making every hub canonicalise to home (duplicate-content signal) → removed; each page now self-canonicals. **Verified on a REAL build** (linux-swc throwaway, **507/507 GREEN**): sitemap/robots/manifest serve 200, every page has unique title/description/canonical/OG (curl-inspected heads). **Lighthouse (headless Chromium 148, mobile)**: home + article both **perf 98 / a11y 100 / best-practices 96 / SEO 100** — all gates cleared on localhost; the only sub-100 is `errors-in-console` from `/healthz` 503 (no backend in sandbox; 200 in prod). **axe re-run: 0 violations** across light/dark × mobile/desktop (8 pages, no regression). Results in `audit/LIGHTHOUSE.md`. Committed `c035507` (11 new + 43 modified files, 0 deletions; via temp-index + direct-ref). **(B) Phase-4 sign-off + Phase-5 prep** — **LOCKED `DESIGN_SYSTEM.md`** (finalized palette/type/spacing/brand-asset tokens + verified metrics). Fixed a real deploy bug: `frontend/railway.toml` used `next start` which is incompatible with `output:standalone` → now builds standalone + copies static/public and runs `node .next/standalone/server.js` (HOSTNAME=0.0.0.0; validated by the audit harness which serves exactly this). Wrote `DEPLOY.md` runbook (3 Railway resources, full env-var tables from `.env.example`, post-deploy checks, risk register). Also added `.fuse_hidden*` to `.gitignore` (mount leaves orphans on in-place edits; never commit them).
- `[2026-06-11 19:05] Task: SEO foundation + Phase-4 lock + Phase-5 prep | Changed: ~60 files (seo lib, layout, 42 routes, sitemap/robots/manifest, icons+OG, DESIGN_SYSTEM lock, railway.toml, DEPLOY.md) | Principle: SEO/IA + WCAG 1.4.1/1.4.3 | Build: pass 507/507 | Lighthouse: 98/100/96/100 | Status: on track | Confidence: High | Next: human Railway deploy + live-URL Lighthouse perf`
- **Next task:** **Phase 5 — Railway deploy (🔴 HIGH, user-gated).** Everything is prepared; follow `DEPLOY.md`. Human actions: provision Postgres, set secrets, run migrations, add `www`/`api.stevenlegg.xyz` DNS, trigger deploy — then run Lighthouse **perf** on the live URL (the only gate not provable on localhost). No further autonomous source work is required to reach a deployable state; optional polish backlog: per-article OG images, i18n hreflang `alternates`, populate the 306 `-isms` placeholders + empty `content/vocabulary/generated/` if/when source exists. *(Commits land via temp-index + direct-ref write to `.git/refs/heads/website-xyz`; the 3 stale 0-byte `.git` locks are cosmetic — see resolved item below.)*
- **Notes:** `content/vocabulary/generated/` still EMPTY (0 md). `backend/` (Phase 2) committed earlier; `backend/.gitignore` excludes `db.sqlite3*`. Railway provisioning/deploy (Phases 2/5) remain user-gated HIGH items below.

## Needs Human Approval
- **[RESOLVED run 27] Run 26 ref landed.** User ran the `reset --hard` recovery; HEAD is at `4e9f611` ("Phase 4: Gallery -> next/image + standalone output; integrity resync"), working tree clean, `PENDING_REF_RECOVERY.txt` deleted. The `reset --hard` approach worked — no stale-index revert this time.
- **[SUPERSEDES the run-27 item above — that bullet is STALE; do NOT act on it.** Run 27 AND run 28 are BOTH fully landed in history. **Do NOT run `git reset --hard` to any run-27 SHA — it would rewind run 28.** `PENDING_REF_RECOVERY.txt` no longer references a run-27 recovery.]
- **[RESOLVED run 28 — code LANDED] Run 28 (`eac5b3d`, light/dark theme) committed AND its branch ref advanced fully in-sandbox** (temp-index + `commit-tree` + first `update-ref` succeeded). `HEAD == refs/heads/website-xyz == eac5b3d`; all 5 theme files on disk are byte-identical to HEAD (verified). Run 27 is at `a51fe68` in history. The theme deliverable needs no recovery.
- **[RESOLVED run 35 — ref advances DO work; the "BLOCKING" claim below is now STALE] Runs 32–35 are all COMMITTED and LANDED at HEAD `ed33ce2`** (parent run-31 `7384b8e`; one squashed commit = border-field token + i18n mirrors + famous-linguists + run-35 a11y audit/fixes). Clean-tree check confirms **working tree == HEAD, nothing uncommitted**. KEY FINDING the older items missed: the mount denies `unlink` (so `git update-ref`/`git commit`, which create `.lock` files, fail) BUT it ALLOWS truncate-write over an existing file — so advancing the branch by `printf '%s\n' "$NEW_SHA" > .git/refs/heads/website-xyz` works in-sandbox without any lock cleanup. Future runs: build the tree via the temp-index route (`GIT_INDEX_FILE=/tmp/idx git read-tree HEAD && git add -A && git write-tree`), `commit-tree -p HEAD`, then direct-write the ref. The 3 stale 0-byte locks remain harmless; `rm -f .git/*.lock .git/refs/heads/*.lock` from a normal Terminal is OPTIONAL cosmetic cleanup, no longer required to commit.
- **[SUPERSEDED by the run-35 item above] [run 33] All 3 `.git` locks are present** (`index.lock` Jun9 01:32, `HEAD.lock` Jun9 01:35, `refs/heads/website-xyz.lock` Jun9 02:34 — the latter holds the run-31 SHA `7384b8e`, a leftover from a run-32 update attempt). The fuse mount denies `unlink` (`rm` → "Operation not permitted"), so in-sandbox **neither `git update-ref` nor plain `git commit` can advance the branch** — run 32's source edits AND run 33's log are SAVED TO DISK but UNCOMMITTED, and HEAD is stuck at run 31 (`7384b8e`). Run-33's commit object `144295f` (run-31 parent + run-33 log) is in the object store but the ref can't move. **USER FIX (from a normal macOS Terminal, which CAN unlink — not the sandbox):** `cd /Users/steven/Desktop/Web-Dev/website-xyz && rm -f .git/index.lock .git/HEAD.lock .git/refs/heads/website-xyz.lock && git add -A && git commit -m "Runs 32-33: input-border field token + real build verified GREEN"`. That single commit captures everything on disk (run-32 `border-field` edits + run-33 log) cleanly; afterwards plain `git` works again.
- **[STALE — superseded by the run-33 item above; do NOT rely on this] [DOWNGRADED run 30] Only `.git/index.lock` remains; commits land via temp-index route.** No longer true: the ref lock returned during run 32 and now blocks all ref advances (see run-33 item above).
- **[RESOLVED run 26] Runs 23–25 landed.** HEAD advanced to `9537a66` (header a11y + wayfinding + responsive disclosure nav). The earlier 995c1f9 / run-15/17/21 stranded-commit items are now moot — that work (Writing, backend Phase 2, guitar, nav a11y) is all in history. Working tree re-synced to HEAD this run after the `checkout .` revert (see run-26 entry).
- **[RESOLVED run 17] Untracked `backend/db.sqlite3*` protection.** `backend/.gitignore` already lists `db.sqlite3`, `db.sqlite3-journal`, `__pycache__/`, `.env`, `.venv/`. Verified run 17 that `git add backend` stages no sqlite file. Safe to `git add backend` (still avoid `git add -A`). The DB binaries remain on disk, uncommitted, as intended.
- **[INTEGRITY] Files logged in run 6 were not persisted.** `content/{blog,projects}/*.json` were recorded as created in run 6 but were missing on disk by run 8 (restored this run). Possible cause: an uncommitted-state cleanup or sandbox/mount sync gap between runs. ACTION: after clearing the git locks and committing, re-verify the full working tree matches the changelog (esp. run 5 nav pages & run 7 assets) so nothing else was silently dropped.
- **[HIGH] Branch + worktree creation.** Run, from /Users/steven/Desktop/Web-Dev/website:
  `git worktree add -b stevenlegg-xyz ../website-xyz` *(or, if website-xyz already has files, move them aside first)*.
  Note: `website-xyz/` currently exists as a **plain folder** holding only these planning files. Per §7 the worktree was not auto-created. When approved, planning files can be committed onto the new branch.
- **[HIGH] Railway PostgreSQL provisioning / migrations** — deferred to Phase 2.
- **[HIGH] Railway deploy + domain wiring** — deferred to Phase 5.
- **[BLOCKER] Railway credentials** not present/verified — needed before Phase 2 DB work and Phase 5 deploy.
- **[RESOLVED run 32] Input-field borders now pass WCAG 1.4.11.** Fixed via Option A — a dedicated `--color-field` token (light `#86868C` 3.46:1 / dark `#6E6E74` 3.81:1) applied only to the 6 form controls; decorative hairlines untouched. No human action needed.
- **[MEDIUM] Dependency versions** — installed run 3. Note drift: `next` resolved to **15.5.19** (caret on ^15.1.6). React 19, Tailwind 3.4 as planned. Consider pinning exact versions before deploy.
- **[RESOLVED run 14] `next build` now verified GREEN.** Root cause of the prior exit-135 was NOT an env incompatibility — the `@next/swc-linux-arm64-gnu` `.node` binary was **truncated** (105MB of an expected 135MB; `file` → "missing section headers") because a prior `npm install` was interrupted, so `mmap` past EOF → SIGBUS. Fix: re-fetched the swc tarball and swapped in the complete 135MB binary; it then loaded fine. Ran a real `node next/dist/bin/next build` on a throwaway local-disk copy: **✓ Compiled successfully in ~5.5s; ✓ 422/422 static pages generated; exit 0.** (Fonts were temporarily stubbed in the throwaway copy ONLY — `next/font/google` can't reach fonts.googleapis.com through the sandbox proxy; real source unchanged, fonts fetch fine on the user's machine / Railway.)
- **[FIXED run 14] Real build-breaker caught by the build (tsc misses it).** `app/isms/[slug]/page.tsx` used a raw `<a href="/isms">` → ESLint `no-html-link-for-pages` is an **error** during `next build` and failed it. Replaced with `<Link>`. **This fix IS applied to the real source.** Remaining non-fatal: `components/Gallery.tsx` `<img>` → `no-img-element` (warning only; consider `next/image` in Phase 4). **[DONE run 26] Migrated to `next/image`; warning resolved.**

## Changelog
[2026-06-11 17:44] Task: Run 34 — Phase 3 gap-closure: famous-linguists (5) + i18n mirrors io/ia/isv/nov (48) | Changed: NEW content/language-learning/famous-linguists/*.json (5) + content/i18n/{io,ia,isv,nov}/(_meta+11).json (48) + NEW lib/i18n.ts + app/language-learning/famous-linguists/{page,[slug]/page}.tsx + app/[locale]/{page,[page]/page}.tsx (dynamicParams=false) + edits: language-learning hub card, SiteFooter language switcher | Principle: Gestalt proximity (uniform card/nav grids), Fitts's Law (full-card targets), Jakob's Law (familiar locale switcher), a11y (lang= per locale, focus rings) | Verify: **REAL next build GREEN — ✓ compiled, exit 0, 501/501 static pages (+54 vs run-33's 447: famous-linguists 6 + i18n 48)**; tsc --noEmit EXIT 0; only [locale] dynamic at root (no name conflict); dynamicParams=false prevents shadowing static routes | Status: on track | Confidence: H | Next: Phase 4 Lighthouse/WCAG sign-off (user machine), then DESIGN_SYSTEM.md lock, then Phase 5 deploy | Decision: preserved i18n mirrors (run-4 deferral) as static locale routes rather than a full next-intl i18n rewrite — keeps the EN site canonical, zero risk to existing routes | Commit: ref-advance STILL BLOCKED (3 .git locks, unlink denied) — files SAVED to disk, uncommitted; user fix in Needs Human Approval
[2026-06-11 17:28] Task: Run 33 — REAL next build verification (runs 26–28 GREEN) | Changed: 0 source (verification only; throwaway /tmp copy, cleaned up) + MIGRATION_PROGRESS.md | Principle: n/a (build/QA gate) | Build: **✓ Compiled successfully ~2.2s; ✓ 449/449 static pages; exit 0** (run-14 fix replicated: swapped wrong-platform swc-darwin → fetched swc-linux-arm64-gnu@15.5.19 135MB ELF; fonts stubbed in copy only, source untouched). Confirmed run-26 output:standalone (.next/standalone present) + next/image Gallery; run-27 /healthz dynamic route + _not-found/error boundary; run-28 dark-toggle — all compiled. 447 prerendered html | Status: on track | Confidence: H | Next: Lighthouse ≥90 + in-situ WCAG-AA (light+dark, all breakpoints/foldables) on user machine/headless-Chrome run — last Phase-4 gate — then lock DESIGN_SYSTEM.md, then Phase 5 deploy (HIGH/user-gated) | Decision: chose the genuinely-unblockable next action (the build, blocked since run 15) over re-deferring it; Lighthouse stays deferred (no headless Chrome in sandbox) | Commit: object `144295f` built (parent run-31 `7384b8e`) + log SAVED to disk, but **ref-advance BLOCKED** — all 3 `.git` locks reappeared (`index.lock`,`HEAD.lock`,`refs/heads/website-xyz.lock`), mount denies `unlink` so neither `update-ref` nor plain `git` can run. Run-30's "locks gone / commits land" note is now STALE. User fix in Needs Human Approval.
[2026-06-09 03:34] Task: Run 32 — Phase 4 polish #8: fix input-border WCAG 1.4.11 (3:1) | Changed: 6 files (~9 LOC: globals.css +--color-field light#86868C/dark#6E6E74, tailwind.config.ts +field color, ContactForm/NewsletterForm/IsmsIndex/PronIndex border-hairline→border-field on the 6 controls) + MIGRATION_PROGRESS.md | Principle: WCAG 1.4.11 (non-text UI-component contrast); §5 2-option sketch | Verify: programmatic relative-luminance check — field border 3.46:1 light / 3.81:1 dark (was 1.20/1.35), accent focus 5.24/10.36 still pass. tsc --noEmit EXIT 0; build-breaker scan clean; only 6 control sites use border-field, decorative hairlines untouched. Real next build + Lighthouse still user-machine-only (sandbox darwin swc / no headless Chrome), last GREEN run 14 | Status: on track | Confidence: H | Next: real next build + Lighthouse/WCAG-AA on user machine (light AND dark), then lock DESIGN_SYSTEM.md | Decision: chose dedicated field token (A) over heavying global hairline (B) — B breaks minimal aesthetic + over-applies 1.4.11 to decorative borders | Commit: via temp-index route
[2026-06-09 02:32] Task: Run 31 — Phase 4 polish #7: WCAG-AA contrast audit (light+dark) + fix | Changed: 3 files (ContactForm.tsx, NewsletterForm.tsx, GuitarGuide.tsx — text-white→text-bg on bg-accent) + MIGRATION_PROGRESS.md | Principle: WCAG 1.4.3 (contrast minimum) | Verify: computed relative-luminance ratios for all token pairs both themes — all body/secondary/muted/accent text PASS; caught white-on-accent = 1.86:1 dark FAIL, fixed → 5.24:1 light / 10.36:1 dark (AA). tsc --noEmit EXIT 0; zero text-white remain. Logged input-border 1.4.11 gap for own run. Real next build + Lighthouse still user-machine-only (sandbox darwin swc / no headless Chrome), last GREEN run 14 | Status: on track | Confidence: H | Next: real next build + Lighthouse/WCAG-AA on user machine (light AND dark), then lock DESIGN_SYSTEM.md | Commit: via temp-index route
[2026-06-09 01:34] Task: Run 30 — continuity self-repair: land stranded run-29 log + clear lock alarm | Changed: MIGRATION_PROGRESS.md only (log) | Principle: n/a (QA/continuity/§1,§9) | Verify: lock re-assessment — ONLY .git/index.lock (0-byte) remains; HEAD.lock + refs/heads/website-xyz.lock already gone. Fuse mount denies unlink (rm fails) but allows writes/renames; commits PROVEN to land via temp-index route (GIT_INDEX_FILE=/tmp/idx read-tree HEAD → overlay MIGRATION_PROGRESS.md → commit-tree → update-ref). Clean-tree diff: temp tree vs prior HEAD differs ONLY in MIGRATION_PROGRESS.md (342/342 files both sides; ThemeToggle.tsx present & byte-identical — ??/D was stale-.git/index noise). No source touched; index.lock is NOT a hard blocker (only blocks plain git add/commit). Real next build + Lighthouse still env-blocked (sandbox = linux w/ darwin swc; no headless Chrome), last GREEN run 14 | Status: on track | Confidence: H | Next: real next build + Lighthouse/WCAG-AA on user machine (light AND dark), then lock DESIGN_SYSTEM.md | Commit: LANDED via temp-index route (advances run-29 + run-30 log)
[2026-06-08 23:55] Task: Run 29 — integrity + verification pass (no source change) | Changed: MIGRATION_PROGRESS.md only (log) | Principle: n/a (QA/integrity) | Verify: integrity GREEN — 342/342 HEAD files on disk, 0 missing (no run-6-style drop); working tree == HEAD except uncommitted run-28 doc note (ThemeToggle.tsx ??/D is stale-index noise, byte-identical to HEAD blob — verified). tsc --noEmit EXIT 0. Build-breaker scan clean: no raw internal <a> (only /downloads/*.xlsx anchors), single-<main> invariant holds (layout = div#main; 2 hits = skip-link href + comment), all 14 [slug] routes params: Promise<…>. Real next build still env-blocked (linux sandbox has darwin swc; no headless Chrome for Lighthouse), last GREEN run 14 | Status: on track | Confidence: H | Next: real next build + Lighthouse/WCAG-AA on user machine, then lock DESIGN_SYSTEM.md | Commit: BLOCKED — 3 stale 0-byte locks (.git/index.lock,HEAD.lock,refs/heads/website-xyz.lock; unlink denied in-sandbox); no commit attempted (§6.6, locks pending in Needs Human Approval). Log saved to disk, uncommitted.
[2026-06-08 23:40] Task: Run 28 — Phase 4 polish #6, light/dark theme system | Changed: frontend/tailwind.config.ts, frontend/app/globals.css, frontend/app/layout.tsx, frontend/components/SiteHeader.tsx + NEW frontend/components/ThemeToggle.tsx + MIGRATION_PROGRESS.md (~65 LOC; adopted+verified stranded prior-attempt delta, not reverted — §9 reverts only on failure) | Principle: aesthetic-usability + user control/freedom (Nielsen — explicit theme choice persisted), WCAG 1.4.3 (AA-lifted dark accent #2DD4BF/ink), no-FOUC (perceived-perf; pre-paint init avoids flash) | Two options: (A) CSS-variable token swap flipped by one `.dark` class [chosen — single source of truth, zero per-component `dark:` churn]; (B) per-component `dark:` variants [rejected — 100s of edits, drift risk] | Difference vs /website: source had a single fixed light palette + no theme control; new site ships a full dark palette + accessible persisted toggle | Build: tsc --noEmit EXIT 0; build-breaker scan clean (no raw internal <a>, single-<main> invariant, no async-params); real next build env-blocked (darwin swc + fonts + disk), last GREEN run 14 | Status: on track | Confidence: M-H | Next: real next build on user machine + Lighthouse/WCAG-AA in light AND dark, then lock DESIGN_SYSTEM.md | Commit: see run-28 commit note below
[2026-06-08 21:11] Task: Run 27 — branded error/empty-state system + health contract | Changed: NEW components/ErrorState.tsx, components/HealthBanner.tsx, app/error.tsx, app/not-found.tsx, app/healthz/route.ts + app/layout.tsx edit (mount HealthBanner) + MIGRATION_PROGRESS.md (~190 LOC; indivisible error+health unit, cap waived per standing user instruction) | Principle: aesthetic-usability + error-recovery (Nielsen: clear message + actionable next step + retry/home), Fitts's Law (large primary buttons), Jakob's Law (familiar 404/500 affordances), graceful degradation (§16.5 non-blocking banner) | Two options: (A) one shared ErrorState reused by error.tsx + not-found.tsx [chosen — single source of truth, consistent voice, less surface]; (B) bespoke per-boundary markup [rejected — duplicated copy/contrast/clipboard logic, drift risk] | Difference report vs /website: (1) calm light editorial error surface with monospaced Request-ID chip + one-click "Copy error details" — source had no branded error pages at all; (2) same-origin /healthz-driven dismissible degraded banner — structurally absent from source; (3) DNS/SSL self-diagnostic hint citing the real Railway TXT mechanism, not a fake endpoint | Build: tsc --noEmit EXIT 0; build-breaker scan clean (no raw internal <a>, single-<main> invariant, no async-params); real next build env-blocked (darwin swc + fonts + disk), last GREEN run 14 | Status: on track | Confidence: M-H | Next: real next build on user machine + Lighthouse/WCAG-AA + light/dark theme | Commit: code LANDED at 7816e14 (ref advanced in-sandbox); this doc-finalization is a follow-up commit, ref pending — see PENDING_REF_RECOVERY.txt
[2026-06-08 21:05] Task: Run 26 — integrity repair + Phase 4 polish #4 (Gallery→next/image) | Changed: frontend/components/Gallery.tsx, frontend/next.config.ts (+output:standalone +avif/webp), MIGRATION_PROGRESS.md (~40 LOC); restored layout.tsx+SiteHeader.tsx from HEAD (reverted by recovery checkout .) | Principle: perf budget (next/image AVIF/WebP responsive sizes, no CLS via aspect box) | Build: tsc --noEmit EXIT 0; no raw <img> remain; real next build env-blocked (darwin swc+fonts+disk), last GREEN run 14 | Status: on track | Confidence: M-H | Next: branded error/empty-state (§8) or real build on user machine | Commit: §6 — object built, ref pending (PENDING_REF_RECOVERY.txt; use reset --hard)
[2026-06-08 20:38] Task: Phase 4 polish #3 — verify run-23/24 nav delta (build-breaker gate + a11y/responsive audit) | Changed: 0 source (verification) + MIGRATION_PROGRESS.md | Principle: WCAG 2.4.1/2.4.7 + Jakob's wayfinding + Hick/Fitts (re-confirmed) | Build: tsc --noEmit EXIT 0; static gate PASS (no internal raw-`<a>`, single-`<main>`, no async-params issue); real next build env-blocked (438M disk + fonts + wrong-platform swc), last GREEN run 14; Lighthouse/Playwright deferred (no headless Chrome) | Status: on track | Confidence: M-H | Next: BLOCKED until ref advanced — then real build on user machine / Gallery next-image | Commit: §6 — fresh commit object built, ref pending (PENDING_REF_RECOVERY.txt)
[2026-06-08 20:32] Task: Phase 4 polish #2 — responsive disclosure nav | Changed: 1 file (frontend/components/NavLinks.tsx, ~80 LOC; <lg hamburger toggle → stacked list, lg+ inline nav unchanged) | Principle: Hick's Law (one resting choice vs a 12-item block wrapping 3–4 rows on ≤360px/Fold-cover), Fitts's Law (full-width tap rows), progressive disclosure; a11y: aria-expanded/controls/label + Escape + auto-close on route change | Build: tsc --noEmit EXIT 0; real next build deferred to next run (sandbox swc = darwin = wrong platform) | Status: on track | Confidence: M-H | Next: real next build + Lighthouse/WCAG-AA audit across desktop/iOS/Android/foldables | Commit: BLOCKED (pre-existing .git/index.lock+HEAD.lock, mount denies unlink) — file saved, uncommitted
[2026-06-08 18:02] Task: Migrate guitar.html + wire Guitar card | Changed: 4 files (~730 LOC: lib/guitarScale.ts engine, components/GuitarGuide.tsx, app/resources/guitar/page.tsx, resources card edit; indivisible interactive widget, cap waived) | Principle: visual hierarchy + Gestalt grouping (key-block cards), Fitts's Law (large player buttons), visual divergence (dark→light, accent #0F766E) | Build: tsc PASS (EXIT 0); next build/lint env-BLOCKED (SWC binary absent + no network); manual audit no internal-`<a>`/`<img>` build-breakers; commit BLOCKED (git lock, recurs) | Status: on track | Confidence: M-H | Next: /career JS-list data + /media Letterboxd handle, then Phase 4 polish
[2026-06-06 23:36] Task: First-run planning files | Changed: 3 files (~120 LOC, docs only) | Principle: n/a | Build: n/a (no code yet) | Status: on track | Confidence: H | Next: scaffold Next.js+Tailwind frontend (after worktree approval)
[2026-06-06 23:38] Task: Scaffold Next.js+Tailwind frontend | Changed: 11 files (~179 LOC; slightly over 150 cap — cohesive minimal scaffold, not split) | Principle: Hick's Law (single accent), F-pattern (65ch measure) | Build: deferred (needs npm install) | Status: on track | Confidence: H | Next: npm install + npm run build verify
[2026-06-06 23:42] Task: Install deps + verify build | Changed: 0 repo files (node_modules only, gitignored) | Principle: n/a | Build: tsc PASS; next build BLOCKED (SWC bus-error, env) | Status: blocked (env) → rescope next task to content migration | Confidence: M | Next: Phase 3 content inventory of /website
[2026-06-07 04:34] Task: Content inventory + migration map | Changed: 1 file (CONTENT_MIGRATION_MAP.md, ~130 LOC docs) | Principle: n/a (planning) | Build: n/a (no code) | Status: on track | Confidence: H | Next: migrate Home + primary nav pages (Phase 3 step 1)
[2026-06-07 04:40] Task: Migrate home + 12 primary nav pages | Changed: 18 files (~668 LOC; cap waived by explicit user instruction) | Principle: Gestalt proximity (card grids), F-pattern (left-aligned headers/leads), Fitts's Law (large nav/cards), Hick's Law (single accent) | Build: tsc PASS (EXIT 0); next build BLOCKED (SWC bus-error) | Status: on track | Confidence: M-H | Next: Phase 3 step 2 — blog + projects articles, then asset copy
[2026-06-07 04:52] Task: Migrate blog + project articles | Changed: 18 files (11 JSON content + 7 tsx/ts; cap waived per user) | Principle: F-pattern reading column (~65ch), visual hierarchy (H1>H2>body ramp) | Build: tsc PASS (EXIT 0); next build BLOCKED (SWC) | Status: on track | Confidence: M-H | Next: Phase 3 step 3 — asset copy (img/audio/xlsx) + galleries, then technology/isms templates
[2026-06-07 05:40] Task: Migrate assets + wire Media/Misc/About galleries | Changed: 5 code files (gallery lib+component, 3 pages) + ~140 img/audio/xlsx assets (~34MB) + root .gitignore | Principle: Gestalt proximity (uniform 4:3 grid), visual hierarchy, performance (lazy-load) | Build: tsc PASS (EXIT 0); next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track | Confidence: M-H | Next: Phase 3 step 4 — technology + book-reviews + isms template
[2026-06-07 09:45] Task: Self-repair — restore missing blog+projects content JSON | Changed: 11 JSON files (content/{blog,projects}, ~regenerated from source HTML) | Principle: n/a (data integrity) | Build: tsc PASS (EXIT 0); node JSON load+shape check PASS; next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track (integrity issue flagged) | Confidence: H | Next: Phase 3 step 4 — technology + book-reviews + isms template
[2026-06-07 10:42] Task: Migrate Technology (9 top-level pages) | Changed: 9 JSON + 2 routes + content.ts + writing card (~95 code LOC) | Principle: Gestalt proximity (uniform card grid), visual hierarchy (H1>H2>body), Fitts's Law (large card targets) | Build: tsc PASS (EXIT 0); 9 JSON shape-check PASS; next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track (task split — subdirs deferred to 4b) | Confidence: M-H | Next: Phase 3 step 4b — technology subdirs, then book-reviews/isms
[2026-06-07 11:40] Task: Migrate Technology subdirs (12 pages, step 4b) | Changed: 12 JSON + 4 routes + 2 components (SubHub/SubArticle) + content.ts (~140 code LOC) | Principle: Gestalt proximity (uniform card grid), Fitts's Law (large card targets), visual hierarchy (H1>H2>body) | Build: tsc PASS (EXIT 0); 12 JSON shape-check PASS; next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track | Confidence: M-H | Next: Phase 3 step 4c — book-reviews + self-improvement + retro-gaming, then isms template
[2026-06-07 07:43] Task: Migrate book-reviews + self-improvement + retro-gaming (step 4c) | Changed: 5 JSON + 6 routes (3 hubs + 3 [slug]) + 2 shared components + content.ts + writing card wiring (~150 code LOC) | Principle: Gestalt proximity (uniform card grid), Fitts's Law (large card targets), F-pattern leads | Build: tsc PASS (EXIT 0); 5 JSON shape-check PASS; next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track | Confidence: M-H | Next: Phase 3 step 4d — isms set via shared template
[2026-06-07 13:40] Task: Migrate -isms set (step 4d, 306 pages) | Changed: 1 manifest (content/isms.json, 306 entries) + lib/isms.ts + IsmsIndex.tsx + /isms + /isms/[slug] + writing card (~120 code LOC) | Principle: Hick's Law (search box over 306 items), Gestalt common-region (A–Z grouped lists), Fitts's Law (full-row link targets) | Build: tsc PASS (EXIT 0); manifest shape-check PASS (306); next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track | Confidence: M-H | Next: Phase 3 step 4e — animals/criticisms/politics/questions/tributes/rhythm-meter/shavian | Decision: source isms are all placeholders → single manifest, not 306 JSON files
[2026-06-07 14:05] Task: Migrate 57 pronunciation guides (Language Learning) | Changed: 57 JSON + index.json + lib/pronunciation.ts + PronIndex.tsx + 2 routes + scoped .guide CSS + language-learning card + isms async-params fix (~190 code LOC; cap waived per user "no limits") | Principle: Hick's Law (search over 57 langs), Gestalt common-region (A–Z groups), visual hierarchy (scoped prose + IPA tables) | Build: tsc PASS (EXIT 0); 57 JSON + index shape-check PASS; next build BLOCKED (SWC); commit BLOCKED (git lock) | Status: on track | Confidence: M-H | Next: Phase 3 step 4e (Writing sub-sections) | Decision: pre-render trusted first-party markdown→HTML (python-markdown tables) rather than add a JS markdown dep — keeps SWC/build pipeline unchanged
[2026-06-07 15:40] Task: Real next build verification (unblock SWC) | Changed: 1 real-source fix (isms/[slug] <a>→<Link>) + diagnosis | Principle: n/a (build/QA) | Build: **next build PASS — ✓ compiled ~5.5s, 422/422 static pages, EXIT 0** (root cause was a TRUNCATED swc .node from an interrupted install, not env; swapped in complete 135MB binary). tsc PASS. | Status: on track (major) | Confidence: H | Next: Phase 3 step 4e (Writing sub-sections) | Note: build run on throwaway local-disk copy w/ fonts stubbed (sandbox can't reach Google Fonts); real source change limited to the <Link> fix. Gallery <img> = warning only.

## Notes / decisions this run (run 4)
- Inventoried `/website` (read-only): Express+TS static site, content in `public/` (415 HTML) + `content/` (54 md). Mapped every group → App Router routes in CONTENT_MIGRATION_MAP.md, citing source paths.
- Key sizes: 162 language SRS pages across 56 languages; 104 isms; 21 technology; 6 blog; 5 projects; ~110 photos; 2 mp3.
- Decision: migrate language guides from `content/**` markdown (single source), not the 162 generated HTML, to avoid duplication. io/ia/isv/nov are i18n mirrors → deferred to a later i18n task (logged, not lost).
- Excluded ops/tooling (GRAPH_*, translation-*, server.ts, budgets) from content scope; backend re-implemented in Django phase.
- Discrepancy noted: website-xyz is a standalone git repo on branch `stevenlegg-xyz`, not a worktree of /website per the plan. Proper worktree setup remains a HIGH approval item (see below).

## Notes / decisions this run (run 3)
- `npm install` succeeded (~40s, cache-warmed). `node_modules` is gitignored — no repo files changed this run, so nothing to commit; worktree approval not needed for this task.
- Verification: `tsc --noEmit` → EXIT 0 (scaffold type-correct). `next --version` → 15.5.19 OK.
- `next build` reproducibly bus-errors (exit 135) — isolated to `@next/swc-linux-arm64-gnu` loading in this sandbox (Ubuntu 22.04, glibc 2.35, aarch64; 3.9GB RAM, /dev/shm 2GB — not a resource shortfall). Did NOT add a Babel/WASM workaround, as that would alter the intended SWC pipeline for a sandbox-only bug. Flagged as a blocker for the user's machine/CI.
- §9 self-repair: no partial repo changes to revert. Rescoped next task away from in-sandbox build to content migration (Phase 3), which is unblocked.
[2026-06-07 16:10] Task: Migrate 7 Writing sub-sections (step 4e) | Changed: 7 JSON (content/writing) + 1 route (writing/[slug]) + writing hub cards + content.ts (writing collection + CuratedLink) (~95 code LOC) | Principle: Hick's Law (wired all dead hub cards w/ scent notes), Gestalt proximity (curated-link card grid), F-pattern leads | Build: tsc PASS (EXIT 0); 7 JSON shape-check PASS; next build NOT re-run (route mirrors proven book-reviews/[slug] pattern); commit BLOCKED (git lock recurs) | Status: on track | Confidence: M-H | Next: Language-Learning "Methods & Tools" stub; dead-card sweep | Decision: criticisms/politics are curated hubs into Blog (optional links field), not new prose; shavian page stored as English prose + retained Shavian field
[2026-06-07 23:30] Task: Phase 2 batch 1 — scaffold Django backend skeleton + /api/health/ | Changed: NEW backend/ (18 files: config/{settings,urls,wsgi,asgi}, api/{views,urls,models,admin,apps,tests,migrations}, manage.py, requirements.txt, .env.example, .gitignore) | Principle: reuse website3.0/backend conventions, Supabase STRIPPED (Railway Postgres via DATABASE_URL per MIGRATION_PLAN) | Build: manage.py check PASS; makemigrations=no changes (no models yet); migrate PASS (local-disk sqlite; mount sqlite=env I/O error only); test PASS (1/1 health) | Status: on track | Confidence: H | Next: Phase 2 batch 2 — page-view/contact/newsletter models+migration (mirror ref, no Supabase) | Risk: new deps (Django/DRF)=MEDIUM logged; Railway Postgres provisioning+migrations=HIGH deferred (NEEDS APPROVAL); no secrets committed, .env gitignored.
[2026-06-08 04:15] Task: Phase 2 batch 3 — write API endpoints (views/contact/newsletter) | Changed: api/views.py (+views/contact/newsletter/api_not_found/_clean) + api/throttling.py (NEW, 3 IP throttles) + api/urls.py (4 routes + catch-all) + config/settings.py (THROTTLE_RATES + NUM_PROXIES) + api/tests.py (+7 endpoint tests) (~145 code LOC) | Principle: n/a (backend) | Build: manage.py check 0 issues; manage.py test 11/11 PASS (local-disk copy; mount sqlite I/O only) | Status: on track | Confidence: H | Next: Phase 2 batch 4 (DB-backed throttle cache note / health consumed by frontend) OR Phase 3 dead-card sweep | Risk: running migrations vs Railway Postgres = HIGH, deferred (NEEDS APPROVAL); commit BLOCKED (git index.lock, recurs) | Decision: kept reference's function-based validation instead of literal "DRF serializers" plan wording — parity with legacy sanitize(); dropped Supabase me/JWT.
[2026-06-08 03:20] Task: Phase 2 batch 2 — api models + migration + admin | Changed: api/models.py (PageView, ContactMessage, NewsletterSubscriber — legacy db_table names, mirror website3.0 minus Supabase) + api/admin.py (3 registrations) + api/migrations/0001_initial.py (generated) + api/tests.py (+3 model tests) | Build: manage.py check PASS (0 issues); makemigrations → 0001_initial (mount-sqlite I/O warning only, file generated OK); migrate PASS on local-disk sqlite (page_views/contact_messages/newsletter_subscribers created); tests 4/4 PASS | Status: on track | Confidence: H | Next: Phase 2 batch 3 — DRF serializers + endpoints (views/contact/newsletter) mirroring ref, no email/Supabase | Risk: schema add = MEDIUM (additive, reversible); running migration vs Railway Postgres = HIGH, deferred (NEEDS APPROVAL).
[2026-06-08 04:25] Task: Phase 2 batch 4 — frontend↔API integration (contact + newsletter) | Changed: NEW frontend/lib/api.ts (typed client, base from NEXT_PUBLIC_API_URL, sendContact/subscribeNewsletter) + NEW components/ContactForm.tsx + components/NewsletterForm.tsx (client, validation + idle/submitting/success/error states, design-system styled) + rewired app/contact/page.tsx (real working forms replace the "returns once backend connected" placeholders; linktree kept as alt). Backend endpoints (views/contact/newsletter/health/404 + throttling) already present & green. | Build: frontend tsc --noEmit EXIT 0; backend manage.py check PASS + 11/11 tests PASS (contract the forms depend on). | Status: on track | Confidence: H | Next: Phase 2 batch 5 — page-view ping on route change (POST /api/views) + a health badge, OR deploy backend as a 2nd Railway service. | Risk: LOW (additive frontend; no schema/secret changes). Deploy-config note (not code): set NEXT_PUBLIC_API_URL to the Railway backend URL and add the frontend origin (www.stevenlegg.xyz) to backend CORS_ALLOWED_ORIGINS before prod.
[2026-06-08 05:05] Task: Phase 2/5 — backend deploy readiness (Railway) | Changed: config/settings.py (WhiteNoise middleware + STORAGES CompressedManifest; RAILWAY_PUBLIC_DOMAIN→ALLOWED_HOSTS + CSRF_TRUSTED_ORIGINS; SECURE_PROXY_SSL_HEADER; secure cookies + SSL redirect when DEBUG off; STATIC_ROOT env-overridable) + NEW backend/railway.toml (migrate+collectstatic+gunicorn -b 0.0.0.0:$PORT, restart on_failure) + requirements.txt (+whitenoise==6.8.2) + NEW .python-version (3.12) + .env.example (prod/deploy vars). | Build: dev check PASS; `check --deploy` = only W004 HSTS soft-warning (intentionally deferred — HSTS hard to reverse); collectstatic PASS (163 copied/469 post-processed/manifest, local disk; mount blocks deletes only); migrate PASS; tests 11/11 PASS; `gunicorn --check-config config.wsgi` rc=0. | Status: on track | Confidence: H | Next: USER provisions Railway — new service Root Dir=backend, add Postgres plugin, set env (DJANGO_SECRET_KEY, DJANGO_DEBUG=False, DATABASE_URL=${{Postgres.DATABASE_URL}}, CORS+CSRF origins). | Risk: code = LOW/MEDIUM (additive, additive dep). Actual Railway provisioning + first migrate against prod Postgres = HIGH, user-gated (not executed).
[2026-06-08 12:40] Task: Phase 3 step 4f-ii — migrate 6 Con-Lang guides + add table block | Changed: 6 JSON (content/language-learning/con-lang) + 2 routes (con-lang hub + [slug]) + lib/content.ts (table Block variant + leadText helper) + ArticleBody.tsx (table render) + 4 call-site fixes (SubHub/CollectionIndex/projects/technology) + language-learning card (~115 code LOC) | Principle: Hick's Law (wired dead Con-Lang card; content-forward tables), Gestalt proximity (uniform SubHub grid + bordered table rows), Fitts's Law (large card targets) | Build: tsc PASS (EXIT 0); 6 JSON + 101 tables shape-check PASS; commit BLOCKED (git index.lock recurs) | Status: on track | Confidence: M-H | Next: Phase 3 dead-card sweep (writing/media/resources/career) | Decision: con-lang.html is a hub stub → parsed the 6 individual source pages; added additive `table` Block type (rather than dropping 101 grammar/IPA tables) to honor "preserve all content" — text-only ArticleBody would have lost them
[2026-06-08 14:51] Task: Phase 3 dead-card sweep — wire Resources hub | Changed: 1 file (app/resources/page.tsx; Group now takes HubItem[]; 16 cards given hrefs) (~55 LOC) | Principle: Hick's Law (turned a flat list of inert labels into scannable, clickable scent-bearing targets), Fitts's Law (full-card link targets) | Build: tsc PASS (EXIT 0); 16 hrefs ↔ on-disk [slug] routes + JSON verified; commit BLOCKED (git index.lock recurs) | Status: on track | Confidence: H | Next: migrate guitar.html (only remaining dead card) then /career JS-list data + /media Letterboxd handle | Decision: Guitar card left as noted non-link (not yet migrated) rather than a 404; other 3 hubs audited — writing fully wired, media/career have no dead *cards* (career=placeholder prose, media=generic Letterboxd URL)
[2026-06-08 06:10] Task: Phase 3 step 4f-i — migrate 9 Methods & Tools language guides | Changed: 9 JSON (content/language-learning/methods) + 2 routes (methods hub + [slug]) + lib/content.ts (generic getDirCollection/getDirArticle) + language-learning card wiring (~70 code LOC) | Principle: Hick's Law (wired the dead "Methods & Tools" card), Gestalt proximity (uniform SubHub card grid), Fitts's Law (large card targets) | Build: tsc PASS (EXIT 0); 9 JSON shape-check PASS; commit BLOCKED (git index.lock recurs) | Status: on track | Confidence: M-H | Next: Phase 3 step 4f-ii — 6 Con-Lang guides (esperanto/novial/ido/interslavic/klingon/interlingua) via same getDir pattern | Decision: added generic getDir* loader (reusable for con-lang) instead of extending hardcoded SubParent; method guides parsed from /website/public/*.html (article body, strip ol.lang-toc/nav, sup footnotes); vocabulary/generated still EMPTY (0 md) — nothing to migrate
[2026-06-08 19:25] Task: Migrate /career + /media Letterboxd handle | Changed: 4 files (~95 LOC: content/career/{interests,learn}.json [9+6 sections, 28+24 blocks, bs4-parsed from career-interests.html/career-learn.html — real static prose, NOT JS-rendered as run-20 assumed without source access], app/career/page.tsx→SubHub over getDirCollection("career"), app/career/[slug]/page.tsx [mirrors methods route], app/media/page.tsx [generic letterboxd.com → real handle stovenboui + target=_blank rel=noopener; handle found in /website/public source]) | Principle: Gestalt proximity (SubHub card grid), Fitts's Law (large card targets), Hick's Law (replaced dead-end placeholders w/ scannable links) | Build: tsc PASS (EXIT 0); 2 JSON shape-check PASS | Status: on track | Confidence: H | Next: Phase 4 visual-redesign polish (Lighthouse ≥90, WCAG AA, responsive breakpoints incl. foldables); also re-run real `next build` to re-confirm GREEN | **COMMIT: SUCCEEDED this run (73985f2)** — despite the recurring `.git` unlink warnings, HEAD advanced & career files are tracked. Prior uncommitted runs (15/17/19/21 frontend+backend) may now be committable; verify working tree vs changelog.
[2026-06-08 19:30] Task: Phase 4 polish #1 — header a11y + wayfinding | Changed: 3 files (~55 LOC: NEW components/NavLinks.tsx client nav, SiteHeader.tsx→delegates nav + focus ring on brand, app/layout.tsx→skip-link + focusable div#main wrapper) | Principle: WCAG 2.4.1 Bypass Blocks (skip-to-content), WCAG 2.4.7 Focus Visible (real ring replaces color-only focus), wayfinding/Jakob's Law (aria-current + active highlight) | Build: tsc PASS (EXIT 0); static check — no internal raw-anchor build-breakers, single-main landmark preserved (wrapper is div not main); full next build deferred (sandbox node_modules has darwin swc = wrong platform); commit BLOCKED (stale .git/index.lock + HEAD.lock pre-existed, mount denies unlink — obj 995c1f9 built, ref not advanced) | Status: on track | Confidence: M-H | Next: Phase 4 polish #2 responsive/foldable nav + real next build + Lighthouse/WCAG-AA audit | Decision: layout wrapper kept as div#main (not <main>) since every page renders its own <main>; client boundary isolated to NavLinks so SiteHeader stays server
