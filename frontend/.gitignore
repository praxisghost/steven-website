# dependencies
/node_modules
# next.js
/.next/
/out/
# production
/build
# misc
.DS_Store
*.pem
# debug
npm-debug.log*
# env files (never commit secrets)
.env
.env*.local
# typescript
*.tsbuildinfo
next-env.d.ts
# NOTE: package-lock.json MUST be committed — Railway needs it for reproducible
# installs (and `npm ci` requires it). Do not re-add it to this ignore list.
